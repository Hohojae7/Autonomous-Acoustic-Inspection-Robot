# OpenCR firmware status

프로젝트 문서는 LED 제어 테이블 주소 51을 사용하는 커스텀 OpenCR 펌웨어를 설명하지만, 최종 소스와 `burger_led_control V260818R3` 바이너리는 기존 폴더에서 발견되지 않았습니다.

`tools/opencr_updater/`에 보관된 `burger.opencr`은 stock `V230127R1` 계열이므로 최종 LED 펌웨어로 표시하지 않습니다. 이후 실제 커스텀 펌웨어를 찾으면 이 폴더에 소스와 버전 정보를 함께 추가해야 합니다.
